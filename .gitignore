/target
/web/node_modules
/web/dist
web/fixtures/.sirius/
bench/fixtures/*.db
bench/fixtures/*.db-*
*.log
.DS_Store

# Hayvenhurst (added by `hayven init`) — generated index + installed skill
.hayven/
.claude/skills/

# Suite event spine — local telemetry, per SUITE_CONTRACTS §2 ("SHOULD gitignore")
.suite/

# Per-machine MCP config (absolute local path to the Catryna server) — each
# clone points at its own install; never commit a personal path.
.mcp.json

# Private strategy docs — kept local only, never in the public repo.
# (Working copies live outside the repo in ../sirius-private-docs/)
/PRD.md
/ROADMAP.md
/TODO.md
/SUITE_CONTRACTS.md
/CONSOLE_V2.md
/TICKET_TO_RECEIPT.md
